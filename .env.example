# aavo 환경 변수 예시
# .env 로 복사 후 값 설정

# Backend
LOG_DIR=logs
API_HOST=0.0.0.0
API_PORT=8000

# PostgreSQL (추후 사용)
# DATABASE_URL=postgresql://user:pass@localhost:5432/aavo

# LLM / API Keys (추후 사용)
# OPENAI_API_KEY=
